{
  "name": "periscope",
  "version": "0.1.0",
  "description": "给纯文本 coding agent 的视觉桥插件（MVP）—— CLI 到视觉模型的最小完整路径",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "commonjs",
  "main": "dist/core/describe.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test 'dist/**/*.test.js'",
    "build:all": "cd engine && pnpm build && cd .. && pnpm build && cd dsh-plugin && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "periscope-engine": "file:./engine"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
