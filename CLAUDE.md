## Agent skills

### Issue tracker

Issues 和 PRD 存放在 GitHub issues（toRolex/periscope），用 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准 triage 角色使用默认标签：needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：仓库根 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
